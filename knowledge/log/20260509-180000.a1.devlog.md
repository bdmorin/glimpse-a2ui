---
type: devlog-fragment
date: 2026-05-09
slice: a1-control-surface
agent: "Claude Opus 4.7"
worktree: worktree-agent-a941d639d5edf5bf3
parent_session: 2026-05-09 orchestrator (post-arc, agent-facing cluster)
---

# slice A1 — Agent Control Surface

## Author / Context

Worktree-isolated sub-agent dispatched against HANDOFF.md item A1. Job: codify the small set of A2UI v0.8 interaction patterns coding agents should use, with worked JSONL examples and expected `userAction` shapes. Foundation for A2 (MCP bridge) and A3 (agent skill); without it, both downstream pieces drift.

## Did

- Verified worktree isolation, rebased on `origin/main`, created `slice-a1-control-surface`.
- Read AGENTS.md, HANDOFF.md (§A1, §B1), INDEX.md, the parallel-agent-log-fragments convention, and a representative cross-section of `test/fixtures/*.jsonl` (button-only, card-text, modal, multiple-choice, checkbox, text-field-form) to anchor catalog examples in shapes the live renderer is known to accept.
- Authored `knowledge/20260509-180000.agent-control-surface.knowledge.md` defining seven canonical patterns: `confirm`, `choice`, `multi-choice`, `free-text`, `status`, `diff-review`, `command-approval`. Each entry has: when-to-use criterion, full JSONL example (surfaceUpdate + dataModelUpdate + beginRendering), expected `userAction` shape, and a fixture-or-gap reference.
- Added an explicit "Trust Boundary" section: catalog is agent-layer guidance, MCP bridge (A2) is the validator. This was the load-bearing framing the dispatch prompt called out.
- Added "Forward References" pointing at A2 / A3 / B1 so the next slices have a concrete contract to consume.
- Surfaced renderer gotchas inline where they affect a pattern (MultipleChoice empty-array crash → `multi-choice`; `.type` vs `.variant` schema/element mismatch → `choice`). Cross-referenced `vendored-renderer-pathologies` rather than restating it.
- Updated `knowledge/INDEX.md` with a new **Agent Surface** section linking the catalog.

## Considered / rejected

- **Add fixtures for the gap patterns this slice.** Rejected: the dispatch prompt explicitly called this scope creep. Listed them as gaps with canonical filenames so a follow-up slice can land them mechanically.
- **Expose every A2UI v0.8 component as a first-class agent primitive (no curated subset).** Rejected; written up as the explicit competing hypothesis in the catalog. Free composition sprawls A2 and A3.
- **Index-style catalog (one paragraph per pattern, link out for examples).** Rejected — agents pattern-match better against in-line copy-paste-modify JSONL than against external links. The catalog optimizes for "agent reads, agent acts" not "human browses."
- **Use `radio` visual for `choice`.** Documented but not relied upon; the vendored renderer drops `MultipleChoice.type` (HANDOFF.md B1). Catalog leans on `maxAllowedSelections:1` as the load-bearing constraint and notes the visual treatment may render as checkboxes until the upstream bug is fixed.

## Open / next

- Gap fixtures (`confirm.jsonl`, `choice.jsonl`, `status.jsonl`, `diff-review.jsonl`, `command-approval.jsonl`) are a clean follow-up slice — pattern names are settled, JSONL skeletons are in the catalog, just need to drop them into `test/fixtures/`, add to the FIXTURES array in `test/visual.mjs`, and bless goldens.
- A2 (MCP bridge) should mirror these pattern names in either tool names (`a2ui.confirm`, …) or in tool argument hints. Decision deferred to A2's slice.
- A3 (agent skill) should consume the criterion column as the seed for its decision table.
