---
type: auditlog-fragment
date: 2026-05-10
slice: c2-multi-surface
agent: "Claude Opus 4.7 (1M)"
worktree: ../glimpse-a2ui-feature-track (sibling-dir, not .claude/worktrees)
parent_session: 2026-05-10 feature-track session
outcome: OK (uncommitted; pending Brian go-ahead)
---

# C2 — Multi-surface support auditlog

## Agent
Claude Opus 4.7 (1M context). Orchestrator session, not worktree-isolated sub-agent — this slice was driven by the main session in a sibling-dir worktree to coexist with a parallel `design-track` session.

## Worktree
`/Users/brahn/src/github.com/bdmorin/glimpse-a2ui-feature-track`
Branch: `session/feature-track`
Base: `origin/main` at `0507ab6` (post `v0.8.7-canvas-control`).

Deliberately created outside `.claude/worktrees/` so Brian can browse it directly and so it is not subject to the agent-scoped lock cleanup that holds the existing `agent-*` worktrees.

## Lineage
- Implements HANDOFF.md C2.
- Built on slice A2 (`src/mcp/a2glimpse-mcp.ts` — MCP bridge, shipped `v0.8.4`).
- Built on slice A3 (`skills/a2glimpse/SKILL.md` — agent skill, shipped `v0.8.6`).
- Coordinated with parallel `design-track` session working slice D — shared substrate `src/a2glimpse-host.html` rotates the visual-golden host hash; merge protocol established (one re-bless against the combined hash).

## Action
- Verified worktree isolation; created `session/feature-track` from `origin/main`.
- Reviewed HANDOFF for pickable scope; confirmed C1/B1/B2 blocked; landed on C2.
- Established three design parameters via AskUserQuestion: stack layout, optional surfaceId filter on `await_action`, auto-grow window sizing. Mid-implementation correction folded auto-grow fully into the page (no agent-facing knob).
- Rewrote `src/mcp/a2glimpse-mcp.ts` for per-surface action queues with optional filter; added `self_check` tool with bridge/child/queue/awaits/last-actions/last-rejections snapshot; added RingBuffer<T> helper for last-N tracking.
- Updated `test/mcp.mjs` from 7-tool → 9-tool, added multi-surface routing assertion (drained `beta` before `alpha`), added self_check shape assertion.
- Edited `src/a2glimpse-host.html`: removed single-surface guard at lines 10928-10930; replaced with visible-stack array + `tick` counter; new `render()` iterates and wraps `__a2glimpse_debug` distinctly; added ResizeObserver-driven content-size ping to Swift; gated `min-height: 100vh` on `body[data-test-mode]` to preserve visual goldens.
- Edited `src/a2glimpse.swift`: added `__a2glimpse_resize_to_content` handler with screen-cap, sub-2px deadband, test-mode and statusItem-mode gates.
- Updated `skills/a2glimpse/SKILL.md`: tool table; new Multi-surface and Debug-surface sections; conventions rewritten.
- Updated `README.md` (constraints + tools).
- Updated `HANDOFF.md` (C2 marked shipped, milestone tags added, design-track-aware merge note).

## Outcome
**DONE on the branch; uncommitted.** All work present on `session/feature-track` as unstaged changes. `npm test` and `npm run test:mcp` both green. No commits yet — pending Brian go-ahead and merge sequencing with `design-track`.

## Artifacts
- `src/mcp/a2glimpse-mcp.ts` (modified) — per-surface queues, filter-aware await, `self_check`.
- `src/a2glimpse-host.html` (modified) — multi-surface stack, auto-grow ping, test-mode `min-height` gate.
- `src/a2glimpse.swift` (modified) — auto-grow handler.
- `test/mcp.mjs` (modified) — assertions for tool count, multi-surface routing, self_check.
- `skills/a2glimpse/SKILL.md` (modified) — multi-surface and debug sections, updated tool table.
- `README.md` (modified) — constraints + tools.
- `HANDOFF.md` (modified) — C2 shipped, milestone tags.
- `knowledge/log/20260510-020000.c2.devlog.md` (this slice's devlog).
- `knowledge/log/20260510-020000.c2.auditlog.md` (this file).
- `test/__snapshots__/e8544dfcd584/button-only.png` (UNTRACKED, working reference) — single golden against this branch's host hash. Not blessed for the full fixture set; design-track's `0ff4d09524c9/` baseline left untouched.

## Commits
None yet. Branch state: 7 modified + 2 new in `knowledge/log/` + 1 untracked snapshot dir.

## Notes
- Coexists with parallel `design-track` session by convention, not by tooling. Both sessions will edit `src/a2glimpse-host.html`; the visual-golden host hash rotates per side. Merge protocol: rebase, resolve conflicts (likely small in `:host` block), then a single `npm run test:visual:update` against the merged hash.
- The `e8544dfcd584/` snapshot directory in this branch is informational — used to eyeball the layout regression that exposed the `:host { min-height }` gate. Can be discarded at merge.
- No new fixtures added; the smoke test exercises one surface, the MCP test exercises two surfaces against the fake child. First multi-surface render against a real Mac is post-merge.
- Trust boundary unchanged: the validator still rejects `html`/`file`/`eval` at any depth and enforces the top-level allowlist.
